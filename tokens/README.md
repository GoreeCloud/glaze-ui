# GLAZE UI V1.0 Tokens

`tokens/glaze-v1.json` is the current V1 token namespace manifest. Supporting unversioned token files provide semantic color, material, layout, state, motion, icon, and accessibility inputs used by the V1 baseline where applicable.

Token presence alone does not establish V1 consumer conformance. Exact-revision validation and product acceptance remain required.
