# GoreeCloud Design Center Static Website

Canonical static-site source lives in `GoreeCloud/goreecloud-static-websites` under `sites/design/`.

The `website/` subtree in this Glaze UI repository is retained only as transitional deployment/history material until the central-site cutover and exact production revision are verified. New authoritative Design Center static-site changes belong in the central repository.

Glaze UI design-system contracts, tokens, components, lifecycle authority, release evidence, and non-site reference material remain authoritative in `GoreeCloud/goreecloud-glaze-ui`; only the public static website source is centralized.

The current Stable Glaze UI product identity is **GLAZE UI V1.6** (`1.6.0`). Current production-readiness and consumer-conformance statements must be backed by exact-revision evidence and lifecycle authority in `registry/lifecycle.json` and `VERSION`.

GLAZE UI `1.5.1` is the retained immediate known-good Stable rollback baseline. Earlier accepted qualification remains historical provenance for its exact scopes and does not override the current V1.6.0 consumer target.

The shared V1.6.0 Stable release does not automatically establish downstream consumer adoption, deployment, production cutover, physical-device acceptance, or Stable acceptance of any consumer.
