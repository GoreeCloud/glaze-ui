# GoreeCloud Design Center Static Website

Canonical static-site source lives in `GoreeCloud/goreecloud-static-websites` under `sites/design/`.

The `website/` subtree in this Glaze UI repository is retained only as transitional deployment/history material until the production cutover to the central repository is verified. New authoritative Design Center static-site changes belong in the central repository.

Glaze UI design-system contracts, tokens, components, lifecycle authority, release evidence, and non-site reference material remain authoritative in `GoreeCloud/goreecloud-glaze-ui`; only the public static website source is centralized.

The current Stable Glaze UI product identity is **GLAZE UI V1.4 — Optical Material & Chromatic Depth** (`1.4.0`). Current production-readiness and consumer-conformance statements must be backed by exact-revision evidence and the lifecycle authority in `registry/lifecycle.json`.

GLAZE UI V1.3 / `1.3.0` is the immediate Stable rollback baseline. Human/manual/subjective/assistive-technology/physical-device qualification deferred from V1.4.0 is tracked under V1.4.1 and remains unverified until real evidence is recorded.
