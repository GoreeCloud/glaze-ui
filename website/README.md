# GoreeCloud Design Center Static Website

Canonical static-site source now lives in `GoreeCloud/goreecloud-static-websites` under `sites/design/`.

The `website/` subtree in this Glaze UI repository is retained only as transitional deployment/history material until the central-site cutover and exact production revision are verified. New authoritative Design Center static-site changes belong in the central repository.

Glaze UI design-system contracts, tokens, components, lifecycle authority, release evidence, and non-site reference material remain authoritative in `GoreeCloud/goreecloud-glaze-ui`; only the public static website source is centralized.

The current Stable Glaze UI product identity is **GLAZE UI V1.5 — Contextual + Capability Awareness** (`1.5.0`). Current production-readiness and consumer-conformance statements must be backed by exact-revision evidence and the lifecycle authority in `registry/lifecycle.json` and `VERSION`.

GLAZE UI V1.4.1 / `1.4.1` is the retained immediately preceding Stable rollback baseline. Its governed shared-design-system human/manual/physical qualification remains historical provenance for the scope it accepted, but it does not override the current V1.5.0 consumer target or automatically certify downstream applications.

V1.5.0 accepts the bounded Stable scope recorded in the canonical repository while deliberately leaving `performance-representative-budget` and `platform-posture-continuity` as V1.5.1 qualification work. This transitional website record therefore must not imply that those V1.5.1 obligations, downstream consumer adoption, deployment, production cutover, or Stable acceptance of any consumer have already been completed.
