# GoreeCloud Design Center Static Website

Canonical static-site source now lives in `GoreeCloud/goreecloud-static-websites` under `sites/design/`.

The `website/` subtree in this Glaze UI repository is retained only as transitional deployment/history material until the central-site cutover and exact production revision are verified. New authoritative Design Center static-site changes belong in the central repository.

Glaze UI design-system contracts, tokens, components, lifecycle authority, release evidence, and non-site reference material remain authoritative in `GoreeCloud/goreecloud-glaze-ui`; only the public static website source is centralized.

The current Stable Glaze UI product identity is **GLAZE UI V1.5 — Contextual + Capability Awareness** (`1.5.1`). Current production-readiness and consumer-conformance statements must be backed by exact-revision evidence and the lifecycle authority in `registry/lifecycle.json` and `VERSION`.

GLAZE UI V1.5.0 / `1.5.0` is the retained immediate Stable rollback baseline. V1.4.1 and earlier accepted qualification remain historical provenance for their exact scopes, but they do not override the current V1.5.1 consumer target or automatically certify downstream applications.

V1.5.1 preserves the reviewed V1.5 presentation and authority implementation and accepts the bounded shared qualification expansions recorded in `contracts/v1.5.1/stable-scope.json` and `acceptance/v1.5.1-stable.md`. This transitional website record must not imply that downstream consumer adoption, deployment, production cutover, physical-device acceptance, or Stable acceptance of any consumer is automatic.
