# GoreeCloud Design Center Static Website

Canonical static-site source lives in `GoreeCloud/goreecloud-static-websites` under `sites/design/`.

The `website/` subtree in this Glaze UI repository is retained only as transitional deployment/history material until Cloudflare Pages cutover to the central repository is fully verified. New authoritative Design Center static-site changes belong in the central static-websites repository.

Glaze UI design-system contracts, tokens, components, lifecycle authority, release evidence, and non-site reference material remain authoritative in `GoreeCloud/goreecloud-glaze-ui`; only the public static website source is centralized.

The current Official Stable Glaze UI product identity is **GLAZE UI V1.4 — Optical Intelligence** (`1.4.0`). Current production-readiness and consumer-conformance statements must follow `VERSION`, `registry/lifecycle.json`, the V1.4 Stable contract, and exact-revision evidence rather than historical website copy.

GLAZE UI V1.3 / `1.3.0` is the immediate known-good Stable rollback baseline. Earlier V1.0–V1.2 release and Candidate material remains historical/provenance evidence only and does not define the current target.

Human optical review, manual assistive-technology verification, representative physical-device/native-platform qualification, real-device performance validation, and subjective polish work deferred from V1.4.0 are tracked under V1.4.1 and must not be represented as passed V1.4.0 evidence.
