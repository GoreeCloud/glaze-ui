# Glaze UI 2.0 Candidate Acceptance Record

## Status

- Design contract: **Enforced**
- Lifecycle: **Candidate**
- Candidate version: **2.0.0**
- Last validated Stable implementation baseline: **1.6.0**
- Production eligibility: **false**
- Effective design-contract date: **2026-08-28**

## Implemented Candidate foundation

This Candidate establishes source-level implementation for the administrator-approved Glaze UI upgrade:

- normative contract in `GLAZE_UI_2.md`;
- machine-readable contract in `tokens/glaze-2.candidate.json`;
- Glaze Material levels Canvas / Surface / Soft Glaze / Glaze / Deep Glaze / Live Glaze;
- new radius, spacing, typography, material, motion, personalization, adaptive-layout, component-family, and accessibility semantics;
- web primitives in `css/glaze-2.candidate.css`;
- representative foldable/hinge-aware primitives in `css/glaze-2.foldable.candidate.css`;
- clarity modes Clear / Balanced / Solid;
- appearance modes Light / Dark / Deep Dark;
- expression modes Calm / Balanced / Expressive;
- Live Glaze pointer/focus response;
- Navigation Capsule scroll-state behavior;
- Connected Transformation API with native View Transition use where available and state-preserving fallback;
- reduced-transparency, reduced-motion, increased-contrast, forced-colors, and no-backdrop fallbacks;
- fail-closed source validation in `scripts/validate_glaze_2_candidate.py`;
- exact-revision CI integration.

## Accepted Candidate evidence

Rendered acceptance is implemented by `reference/candidate-2.0.html`, `reference/candidate-2.0-acceptance.html`, `scripts/validate_candidate_2_rendered.py`, `reference/candidate-2.0-resilience.html`, `reference/candidate-2.0-resilience-acceptance.html`, `scripts/validate_candidate_2_resilience.py`, and `scripts/validate_candidate_2_contrast.mjs`.

Exact-revision CI run `33158551554` passed the full retained Stable suites and all currently implemented Glaze UI 2.0 Candidate gates at revision `e815da3c28e654ae8192d45b001e371e0dd65d21`.

The accepted rendered matrix establishes:

- Light and Dark appearance across Mobile, Tablet, Desktop, Wide Desktop, and TV;
- Deep Dark on representative mobile and desktop surfaces;
- Clear, Balanced, and Solid material clarity;
- Calm, Balanced, and Expressive expression states;
- solid content planes with Glaze interaction surfaces;
- representative Connected Transformation continuity;
- Navigation Capsule mobile reachability, selected-state expansion, and forward-scroll compression without violating the 48px touch floor;
- Tablet rail, Desktop/Wide Desktop persistent navigation, and TV far-view navigation adaptation;
- large-text reflow without horizontal scrolling in the representative mobile case;
- reduced-motion behavior;
- reduced-transparency opaque fallback;
- forced-colors boundaries and opaque material fallback;
- no-`backdrop-filter` fallback;
- 48px general and 56px TV reference interaction floors.

The resilience acceptance additionally establishes:

- Connected Transformation preserves semantic state and reveals the destination when the View Transition API is explicitly unavailable;
- temporary transition naming is cleaned up after the fallback path;
- a representative 1114×834 book-posture foldable layout reserves a 24px hinge exclusion region and keeps primary and secondary panes out of that physical region;
- representative foldable panes retain at least the Candidate's 280px pane floor in that posture;
- `prefers-contrast: more` is rendered independently from forced-colors through Chrome DevTools Protocol media emulation;
- the increased-contrast case strengthens the reference boundary and outline while the core Glaze material increases its computed border to at least 2px;
- the increased-contrast case preserves layout without horizontal overflow.

The rendered gates identified and caused correction of a real reachability defect: whole-capsule forward-scroll scaling reduced nominal 48px targets to about 45px. Compression now reduces Navigation Capsule spacing/chrome instead of shrinking interaction targets.

## Dependency, privacy, and security boundary

The Candidate implementation remains dependency-free at runtime and self-contained in the repository. `scripts/validate_glaze_2_candidate.py` fails closed on:

- browser network APIs used by the Candidate runtime;
- browser storage/cookie/cache APIs;
- common analytics/telemetry SDK markers;
- unsafe dynamic-code and HTML-write primitives;
- remote HTTP(S), protocol-relative, CDN, and CSS import dependencies in Candidate CSS/runtime/reference artifacts;
- drift away from the repository-local Candidate CSS and JavaScript imports used by the rendered reference.

These checks establish the design-system Candidate's dependency/privacy/security boundary; they do not replace application-specific security or privacy acceptance for downstream consumers.

## Consumer migration assessment

`consumers/registry.json` remains the **Glaze UI 1.6 Stable enforcement registry** and is intentionally separate from 2.0 Candidate compatibility claims. Its August 28 reconciliation was fail-closed against current repository-local evidence rather than upgrading applications by declaration.

Current downstream assessment:

| Consumer | Current evidenced baseline | 2.0 assessment |
| --- | --- | --- |
| GoreeCloud Manager | 1.3 Stable mapping | Migration required; no 2.0 evidence. |
| GoreeCloud Website | 1.5 Stable historical mapping | Migration required; no 2.0 evidence. |
| GoreeCloud Tasks | 1.3 Stable mapping | Migration required; no 2.0 evidence. |
| GoreeCloud Launcher | 1.6 Adoption Candidate | Migration required after 2.0 promotion/adoption work; no 2.0 evidence. |
| GoreeCloud Keyboard | 1.6 Adoption Candidate | Migration required after 2.0 promotion/adoption work; no 2.0 evidence. |
| GoreeCloud Notes | Unverified; former central evidence path is absent on current main | Fresh versioned Glaze implementation evidence is required; no 2.0 evidence. |
| GoreeCloud Monitor | Legacy 1.0 historical mapping | Migration required; no 2.0 evidence. |

The central Stable registry now reflects Website's repository-local 1.5 record, Launcher and Keyboard's reviewed canonical 1.6 Stable promotion revision, and Notes' current evidence gap. No downstream consumer is claimed 2.0-conformant by this Candidate record. Each application remains responsible for its own rendered/native/accessibility acceptance and substantive platform-system boundaries.

## Explicitly not yet accepted

This record does **not** establish Stable promotion or downstream application conformance. Promotion remains blocked pending applicable evidence for:

- wearable/spatial implementation acceptance before either becomes a Stable production target;
- final exact Candidate CI evidence after the reconciled acceptance and Stable consumer-audit records are frozen;
- formal Stable promotion and version-state synchronization.

Wearable and spatial remain design-contract targets, not production-ready Glaze UI 2.0 implementations. Stable promotion must not imply those environments are supported until their own implementation and acceptance evidence exists or the formal Stable scope explicitly excludes them without weakening an applicable GoreeCloud product requirement.

## Authority boundary

Glaze UI 2.0 governs presentation and interaction. Its intelligence, Live Surface, security, privacy, resilience, and Mesh presentation patterns do not create underlying execution authority, evidence, or domain truth. Wardveil Security, Privacy Shield, Everkeep, GoreeCloud Mesh, and application logic remain authoritative for their respective substantive state.

## Consumer boundary

The 2.0 design contract is the enforced active-development migration target. Glaze UI 1.6.0 remains the last validated Stable implementation baseline until 2.0 promotion. A consumer adopting 2.0 Candidate behavior is not production-eligible on that basis and must complete normal application-specific acceptance after the design-system release is promoted.
